
// import { GoogleUser } from "@/types/google";
// import { z } from "zod";

// export const schema = z.object({
//   email: z.string().email(),
//   name: z.string(),
//   image: z.string().url(),
// });

// export function validateUserData(user: GoogleUser) {
//   return schema.parse(user); // Throws an error if validation fails
// }
